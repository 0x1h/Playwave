import { FC } from 'react'

interface Props {
    img_url: string;
    name: string
    opacity: number;
}

const PlaylistNavBar = () => {
    return (
        <div className="playlist-nav">
            
        </div>
    )
}

export default PlaylistNavBar
